Public

/ → SSG or ISR
/catalog → SSG or ISR
/catalog/:slug → SSG or ISR
/blog → SSG or ISR
/blog/:slug → SSG or ISR
/search?q=... → SSR or client-side fetch
Auth

/login, /logout → SSR or client-only (no SSG)
Admin

/admin/\* → SSR or client-only (no SSG)
